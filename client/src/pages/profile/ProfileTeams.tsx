function ProfileTeams() {
  return (
    <div>
      <ul className="p-4">
        {[1, 2, 3, 4].map((list) => (
          <li key={list} className="mb-8">
            <div>
 
            </div>
          </li>
        ))}
      </ul>
    </div>
  );
}

export default ProfileTeams;
